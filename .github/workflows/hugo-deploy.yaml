name: "GitHub Pages - Hugo"

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docs:
    # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
    permissions:
      contents: read
      pages: write
      id-token: write

    uses: darbiadev/.github/.github/workflows/github-pages-go-hugo.yaml@91dda16028f109ac78016dd740206fd109740068 # v15.0.0
