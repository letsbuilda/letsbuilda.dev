name: "GitHub Pages - Hugo"

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docs:
    # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
    permissions:
      contents: read
      pages: write
      id-token: write

    uses: darbiadev/.github/.github/workflows/github-pages-go-hugo.yaml@ea97d99e1520c46080c4c9032a69552e491474ac # v13.0.0
